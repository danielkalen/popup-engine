{
  "name": "@danielkalen/popup-engine",
  "version": "3.0.2",
  "description": "Popup creation and interaction engine",
  "main": "src/popup-engine.coffee",
  "scripts": {
    "postversion": "echo \\'$(json -f package.json version)\\' > .version.coffee && npm run build",
    "build": "npm run build:pretty && npm run build:ugly",
    "build:ugly": "simplyimport -i src/popup-engine.coffee | coffee -b -s -c > dist/js/popup-engine.pretty.js && uglifyjs dist/js/popup-engine.pretty.js -m -c keep_fargs,unused=false -o dist/js/popup-engine.js",
    "build:pretty": "simplyimport -i src/popup-engine.coffee | coffee -b -s -c > dist/js/popup-engine.pretty.js && cp -f dist/js/popup-engine.pretty.js dist/js/popup-engine.js",
    "watch": "simplywatch -g 'src/*' -x 'npm run build:pretty -s'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danielkalen/popup-engine.coffee.git"
  },
  "author": "danielkalen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/danielkalen/popup-engine.coffee/issues"
  },
  "homepage": "https://github.com/danielkalen/popup-engine.coffee#readme",
  "peerDependencies": {
    "jquery": "*"
  },
  "devDependencies": {
    "closure-compiler-service": "^0.6.1",
    "coffee-script": "^1.10.0",
    "json": "^9.0.4",
    "simplyimport": "^2.3.3",
    "simplywatch": "^2.3.2",
    "uglify-js": "^2.7.0"
  }
}
